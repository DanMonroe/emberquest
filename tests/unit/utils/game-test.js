import game from 'emberquest/utils/game';
import { module, test } from 'qunit';

module('Unit | Utility | game', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = game();
    assert.ok(result);
  });
});
